{
  "name": "pawparadise",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "bash scripts/setup-db.sh",
    "install:all": "cd backend && npm install && cd ../storefront && npm install && cd ../admin && npm install",
    "dev": "concurrently -n api,store,admin -c blue,green,magenta \"npm run dev:api\" \"npm run dev:store\" \"npm run dev:admin\"",
    "dev:store": "cd storefront && npm run dev",
    "dev:admin": "cd admin && npm run dev",
    "dev:api": "cd backend && npm run dev",
    "build:store": "cd storefront && npm run build",
    "build:admin": "cd admin && npm run build",
    "build:api": "cd backend && npm run build",
    "build:all": "npm run build:api && npm run build:store && npm run build:admin",
    "db:migrate": "cd backend && npm run db:migrate",
    "db:seed": "cd backend && npm run db:seed",
    "db:reset": "cd backend && npm run db:reset",
    "db:studio": "cd backend && npm run db:studio"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
